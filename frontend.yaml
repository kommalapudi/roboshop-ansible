- name: configure frontend
  hosts: frontend
  become: yes
  tasks:
  - name: enable nginx 1.24
    ansible.builtin.command: dnf module enable nginx:1.24
  
  - name: install nginx
    ansible.builtin.dnf:
      name: 